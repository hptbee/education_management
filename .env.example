# Optional Cloudflare Worker URL for automatic classroom backup.
# Leave empty to disable cloud backup (local JSON still works).
NEXT_PUBLIC_CLOUD_BACKUP_URL=

# Optional bearer token — must match BACKUP_API_TOKEN on the Worker.
# Sent as Authorization: Bearer <token> on backup upload.
NEXT_PUBLIC_CLOUD_BACKUP_TOKEN=
