---
description: Repository audit by area with severity-ranked findings
---

Audit:

$ARGUMENTS

Read and follow `.cursor/skills/repository-audit/SKILL.md`.

Do not modify code.

Analyze the repository systematically.

When useful, delegate independent areas:
- Frontend (`src/app/`, `components/`)
- Persistence (`src/database/`, `src/store/`)
- Shell & navigation
- Security (local-first)
- Desktop (`src-tauri/`)

Validate findings before reporting.

Prioritize: Critical → High → Medium → Low
